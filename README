# Compilation using terminal Unix:

mkdir build
cd build
cmake -DCMAKE_PREFIX_PATH=<> -DCMAKE_BUILD_TYPE=Release ..
make


# Executables (in build):

./price0 "path to data_input"

./hedge "path to market_file" "path to data_input"